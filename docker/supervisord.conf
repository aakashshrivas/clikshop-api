[supervisord]
nodaemon=true

[program:laravel-setup]
command=/var/www/html/docker/startup.sh
autostart=true
autorestart=false
priority=5
user=www-data             ; <-- ADD THIS LINE

[program:php-server]
; 🚨 Change to a production server command (e.g., RoadRunner or a PHP-FPM call)
command=php artisan serve --host=0.0.0.0 --port=%(ENV_PORT)s 
autostart=true
autorestart=true
priority=10
user=www-data             ; <-- ADD THIS LINE